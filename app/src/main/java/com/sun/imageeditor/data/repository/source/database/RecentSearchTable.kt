package com.sun.imageeditor.data.repository.source.database

object RecentSearchTable {
    const val NAME = "recent_search"

    object Cols {
        const val ID = "_id"
        const val QUERY = "query"
    }
}
